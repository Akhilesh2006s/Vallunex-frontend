/** @type {import('postcss-load-config').Config} */
const config = {
  // Define PostCSS plugins for this Vite project here if needed.
  // Leaving this empty ensures we don't accidentally pick up the
  // Tailwind-specific PostCSS config from the parent E:\ directory.
  plugins: {
    // Example: autoprefixer: {},
  },
};

export default config;

